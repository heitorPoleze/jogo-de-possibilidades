export function ou0ou1(max, verificacao) {
    const resultado = Math.floor(Math.random() * max);
    verificacao = resultado;
    return resultado;
  }

export function entre0e1(){
    const result = Math.random();
    return result;
}
